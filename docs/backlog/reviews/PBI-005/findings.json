{
  "schema_version": "1",
  "audit": {
    "agent": "code-reviewer",
    "as_of_date": "2026-09-12",
    "mode": "diff",
    "scope": "Round 2 (final) of the uncommitted change on pbi/PBI-005-local-server in C:/Users/jdk/dispatch-board-worktrees/PBI-005: local/server.py, local/tests/test_server.py, local/tests/test_server_live.py, local/tests/server_support.py, local/tests/test_config_local.py, board.config.json, exporters/board_config.py. Intent rung 1 (plan-grade): the approved per-PBI spec docs/backlog/specs/pbi-005-local-server.md revision 2 and the 15 acceptance criteria on docs/backlog/pbi/PBI-005.md (AC-67, AC-68, AC-69, AC-SV1-AC-SV12), with the two spec-gate reviews as settled design; plus the twelve round-1 findings in docs/backlog/reviews/PBI-005/findings-r1.json as the remediation contract. Grounding path: all three configured suites run read-only by the reviewer in the worktree, plus instrumentation and mutation probes - queue.Queue.put_nowait instrumented, the bounded queue swapped for an unbounded one, the round-1 watcher-race probe re-run, each of the eight named fixes mutated back, and an eight-case token-echo sweep over raw sockets. No Touchstone SARIF supplied.",
    "summary": "0 CRITICAL, 0 HIGH, 0 MEDIUM, 3 LOW and 1 informational assessment. Verdict GO. Change class: standard. All twelve round-1 findings resolved except CR-005-12, deferred legitimately (its only remediation is an edit to the approved, gate-passed spec, outside allowed_areas and the owner's call; behaviour pinned instead by a pre-gate Expect: 100-continue test). Re-measured rather than taken on trust: instrumenting queue.Queue.put_nowait recorded 2 queue.Full events (1 per test) where round 1 recorded 0, so the drop path executes and is asserted, and replacing the bound with an unbounded queue fails BOTH tests; the round-1 watcher-race probe went from clients=1 / watcher alive=False / 0 refreshes to alive=True / 11 refreshes with the change delivered, two generations briefly coexisting but the old one exiting on its own stale Event - no orphan, no leak, no sleep-or-retry paper-over; each of the eight remaining named fixes was mutated back and its test failed without it. The builder's new, previously unreviewed HTTP/0.9 fix is correct: all six framework-raised paths (400 malformed, 400 bad version, 414, 431, 501, 505) return a proper status line and 9 headers with no echo of any token in body or headers, Connection: close still honoured, and better than the builder feared - CPython 3.14 gives an HTTP/0.9 request headers = {} so it cannot pass the Host gate, meaning a 0.9 client gets 403 and never any board data. Security surface re-verified independently after the response-path changes: 127.0.0.1/AF_INET with allow_reuse_address=False; the six-method funnel; zero echo across an eight-case sweep; no Access-Control-Allow-*; query_only=1 with file bytes, mtime, data_version and sqlite_master unchanged; AC-SV7 clean on snapshot AND stream; no reset loop. Suites run by this reviewer in the worktree: 307 / 96 / 261, 0 skipped, matching the builder exactly. All 14 new local tests assert something real; none is assertion-free and none weakens a prior assertion - the poll-counter assertion round 1 said was dropped is back. Flag-only review: nothing edited, staged, committed or pushed.",
    "sensitivity": "sensitive",
    "verdict": "GO",
    "round_1_rollup": "CR-005-01 resolved; -02 resolved; -03 resolved; -04 resolved; -05 resolved; -06 resolved; -07 resolved; -08(a,b,c) resolved; -09 resolved; -10 resolved; -11 resolved; -12 deferred, legitimately.",
    "provenance": "ENVELOPE ASSEMBLED BY THE ORCHESTRATOR, NOT EMITTED BY THE REVIEWER. The orchestrator's review prompt wrongly instructed the reviewer to write findings.json as a bare array of {id, severity, title, file, line, finding, remediation}, overriding this agent's real schema; the push rail validates the file fail-closed and refused twice, naming the required shape. The reviewer's literal output is preserved unaltered at docs/backlog/reviews/PBI-005/findings-r2-array.json. Mapping applied here is mechanical: severity upper-cased, file+line folded into `subject`, and the reviewer's `finding` prose carried BYTE FOR BYTE into evidence[0].value. `impact` is a VERBATIM EXCERPT of that same prose - the sentences in which the reviewer itself stated impact - never a paraphrase. `audit`, `not_audited` and `follow_ups` restate the reviewer's reported result; no verdict, severity, count or measurement was changed and nothing was added that the reviewer did not state. One deviation worth naming: the reviewer's fourth item CR2-005-03 is severity Informational, which this schema's severity enum (CRITICAL/HIGH/MEDIUM/LOW) cannot express; rather than inflate it to LOW it is carried in full under follow_ups and in findings-r2-array.json, so it is recorded rather than lost. SendMessage is disabled in this session, so the reviewer could not be asked to re-emit the file itself."
  },
  "findings": [
    {
      "id": "CR2-005-01",
      "severity": "LOW",
      "title": "Spec section 4.3 promises the framework's own diagnostic on stderr, but the send_error override means log_error is never called at all",
      "subject": {
        "type": "file",
        "id": "local/server.py:357",
        "name": "send_error override / log_error never reached"
      },
      "evidence": [
        {
          "field": "local/server.py:357",
          "value": "Section 4.3 states that log_error still writes the framework's own message, including the offending token, to stderr, and calls it the owner's diagnostic. That is not what happens. log_error is invoked only from BaseHTTPRequestHandler.send_error; because send_error is overridden here (server.py:357-359) and goes straight to _respond_plain, log_error is never reached. I instrumented Handler.log_error and drove a 505, a 501 and a 400 over raw sockets: 0 invocations. The spec sentence therefore describes behaviour that the design it also mandates makes impossible - a plan defect rather than a build defect, since the override is exactly what sections 4.3 and 4.3.1 require. Impact is small, and was reduced further by the CR-005-11 fix: log_request now writes a line of the form 'server: 127.0.0.1 - <repr of the request line> <status> -' for every refusal, so the owner still gets the request line (which carries the offending method token or version) and the status code. What is lost is CPython's explanatory wording, for example 'Invalid HTTP version (2.5)'; and on the 414 path the request line has already been blanked to the empty string by handle_one_request, so a 414 logs only that it happened. PBI-007 captures this stderr to the owner's log file, which is the consumer that cares. The builder found and disclosed this itself (change-report-r2, follow-up 3); it was not in the round-1 findings and it is not a regression - it has been true since round 1.",
          "sensitive": false
        }
      ],
      "impact": "Impact is small, and was reduced further by the CR-005-11 fix: log_request now writes a line of the form 'server: 127.0.0.1 - <repr of the request line> <status> -' for every refusal, so the owner still gets the request line (which carries the offending method token or version) and the status code. What is lost is CPython's explanatory wording, for example 'Invalid HTTP version (2.5)'; and on the 414 path the request line has already been blanked to the empty string by handle_one_request, so a 414 logs only that it happened. PBI-007 captures this stderr to the owner's log file, which is the consumer that cares.",
      "remediation": "The owner's call, and cheapest as a spec edit rather than a code change: at the next revision of docs/backlog/specs/pbi-005-local-server.md, reword section 4.3's log_error sentence to say that the overridden send_error emits the diagnostic through log_request (the repr-escaped request line plus the status) and that CPython's own explanatory message is deliberately not kept. If the wording is wanted instead, the override can call self.log_error before _respond_plain - but that would put framework text on stderr that the CR-005-11 escaping does not cover, so the spec edit is the better option. Non-blocking.",
      "refs": [
        "PBI-005 spec section 4.3",
        "AC-SV2"
      ],
      "category": "correctness"
    },
    {
      "id": "CR2-005-02",
      "severity": "LOW",
      "title": "Section 6.3's rule that any unexpected exception in a handler returns 500 is not honoured for an OSError, and the new test now pins the no-response behaviour as the contract",
      "subject": {
        "type": "file",
        "id": "local/server.py:369",
        "name": "_handle except OSError clause"
      },
      "evidence": [
        {
          "field": "local/server.py:369",
          "value": "The except OSError clause in _handle (server.py:369-374) sits above except Exception, so an OSError raised anywhere inside routing - not only a broken pipe - is logged and swallowed with no response written. Section 6.3's table says any unexpected exception in a handler yields 500 with the line 'the server failed to handle this request'. The new test Errors.test_an_os_error_inside_a_handler_is_named_on_stderr (test_server.py:606-615) asserts that the raw response is empty, which turns 'no response at all' from an accident into a pinned contract. This is explicitly one of the two remediations my own round-1 CR-005-03 offered (keep a single except OSError here but emit one warn line naming the errno before passing), and the observability half I actually raised is genuinely fixed: I mutated server.warn to drop that line and the test fails with 'errno 13 not found in'. So this is recorded for completeness, not as a re-raise and not as a moved goalpost. In practice the clause is almost always right: the only OSErrors reachable in that block are socket writes from _serve_page and _serve_snapshot, which have no try of their own, and for those a dropped connection is the correct answer because the peer has gone. A genuine non-connection OSError (a file-descriptor or disk fault) is the case where a 500 would be better than a silent close, and the owner now at least gets the errno on stderr.",
          "sensitive": false
        }
      ],
      "impact": "In practice the clause is almost always right: the only OSErrors reachable in that block are socket writes from _serve_page and _serve_snapshot, which have no try of their own, and for those a dropped connection is the correct answer because the peer has gone. A genuine non-connection OSError (a file-descriptor or disk fault) is the case where a 500 would be better than a silent close, and the owner now at least gets the errno on stderr.",
      "remediation": "Either narrow the clause to the connection-loss errnos (EPIPE, ECONNRESET, ECONNABORTED, ETIMEDOUT and the WSAE equivalents on Windows) and let except Exception answer 500 for the rest, or leave the code as it is and add a sentence to section 6.3 saying an OSError is treated as connection loss and answered with a close plus one stderr line rather than a 500. Either way, loosen the test so it pins the stderr line rather than the absence of a response. Non-blocking; a later pass.",
      "refs": [
        "PBI-005 spec section 6.3"
      ],
      "category": "correctness"
    },
    {
      "id": "CR2-005-04",
      "severity": "LOW",
      "title": "Leftover unused state in the live-test fixture",
      "subject": {
        "type": "file",
        "id": "local/tests/test_server_live.py:21",
        "name": "Live.setUp leftover _orig_poll"
      },
      "evidence": [
        {
          "field": "local/tests/test_server_live.py:21",
          "value": "Live.setUp captures both server.POLL_DEFAULT and server.HEARTBEAT, into self._orig_poll and self._orig_heartbeat. Only _orig_heartbeat is used (line 23 restores it through addCleanup); _orig_poll is never read, and POLL_DEFAULT is never patched by this class - the poll interval is passed per server through start(poll=...). It reads as if a module constant were being saved for restoration when it is not, which is the kind of half-removed scaffolding that later misleads someone into thinking POLL_DEFAULT is mutated somewhere. Catalogue rule: Dead Code and Leftover State.",
          "sensitive": false
        }
      ],
      "impact": "It reads as if a module constant were being saved for restoration when it is not, which is the kind of half-removed scaffolding that later misleads someone into thinking POLL_DEFAULT is mutated somewhere.",
      "remediation": "Drop _orig_poll and assign self._orig_heartbeat alone.",
      "refs": [
        "Maintainability: Dead Code and Leftover State"
      ],
      "category": "maintainability"
    }
  ],
  "not_audited": [
    {
      "ref": "Touchstone SARIF and SAST",
      "reason": "none provided and no SAST configured, so the security dimension is reasoning-plus-probe rather than tool-driven. The reviewer compensated with raw-socket probes over all six framework-raised paths and an eight-case token-echo sweep through Host, Origin, method, request line, version and a header name, in both body and headers."
    },
    {
      "ref": "Python 3.11 and 3.12 interpreters",
      "reason": "not available in this environment. Named in CR2-005-03: on an older supported interpreter (the suite's stated floor is 3.11) whose HTTP/0.9 branch parsed headers, a local process could pass the Host gate over HTTP/0.9 and receive a 200 whose status line, Content-Type, nosniff and CSP were all suppressed, because the version forcing lives in _respond_plain and not in the three success writers. Unreachable on CPython 3.14; a bounded risk, not a measured defect, with no privilege gain since it needs a process already on the machine that could read the database file directly."
    },
    {
      "ref": "AC-68 as a live browser demonstration",
      "reason": "AC-68 is a demonstration criterion (a finish appended to a running agent's transcript reaching an open page within 10 minutes without a reload). The reviewer exercised the server-level equivalent through the live test suite rather than by driving a real browser."
    },
    {
      "ref": "The published artifact and the live store",
      "reason": "read-only review: nothing was pushed, published or refreshed against the live board."
    }
  ],
  "follow_ups": [
    {
      "ref": "CR2-005-03 (Informational, carried here because the severity enum has no INFORMATIONAL) - The HTTP/0.9 version forcing is applied only in _respond_plain, not in the three success writers - unreachable on the shipped interpreter, recorded so it is not lost",
      "detail": "This is the assessment of the builder's new, previously unreviewed defect fix, and the fix is correct. I verified CPython 3.14.5's parse_request: self.request_version is assigned only after the 505 and bad-version-400 checks, so it is still HTTP/0.9 when send_error runs, and send_response_only, send_header and end_headers all gate on request_version differing from HTTP/0.9 and emit nothing. _respond_plain (server.py:440-441) now forces the version, and over raw sockets all six framework-raised paths - 400 (malformed line), 400 (bad version), 414, 431, 501 and 505 - come back with a correct status line and 9 headers: Content-Type text/plain; charset=utf-8, Cache-Control no-store, nosniff, a Content-Length equal to the body, and Connection: close. The connection still closes, because send_header sets close_connection outside the version gate, so nothing is left half-open. The status line uses protocol_version, not the client's claimed version, so nothing new is echoed: I swept a marker token through the Host, Origin, method, request line, version and a header name, and found it in neither the body nor the headers of any of eight responses. Mutating the forcing back to a no-op makes GateFramework.test_every_refusal_has_the_same_plain_shape fail, so the fix is pinned by a test that bites. On the builder's own question - whether a genuine HTTP/0.9 client now receives headers it does not understand - the answer on this interpreter is better than it feared: CPython 3.14 sets self.headers to an empty dict and returns immediately for a two-word request line, on the stated grounds that HTTP/0.9 expects no headers at all. Such a request therefore carries no Host header, is refused 403 by the gate, and can never reach a route. I confirmed that empirically: GET / and GET /api/snapshot over HTTP/0.9, even with a Host header supplied, both return 403 and no board data. The trade is acceptable, and AC-SV2's uniform shape is the right thing to optimise for on a server that binds 127.0.0.1 and serves a browser. The only residue is an asymmetry: the forcing lives in _respond_plain but not in _serve_page, _serve_snapshot or _serve_events. On CPython 3.14 that is unreachable. On an older supported interpreter (the suite's stated floor is 3.11) whose HTTP/0.9 branch parsed headers, a local process could pass the gate over HTTP/0.9 and receive a 200 whose status line, Content-Type, nosniff and CSP were all suppressed. I had no 3.11 interpreter to test here, so this is a bounded risk, not a measured defect; either way it needs a process already running on the machine, which can read the database file directly, so there is no privilege gain.",
      "remediation": "No change required for merge. If it is wanted for tidiness and for older interpreters, hoist the two-line version check out of _respond_plain into a small helper called at the top of _respond_plain, _serve_page, _serve_snapshot and _serve_events, so one rule covers every response this module writes. Worth one sentence in section 4.3 recording why the version is forced at all."
    },
    {
      "ref": "CR2-005-01 (LOW) - spec section 4.3's log_error sentence describes behaviour the override it also mandates makes impossible. Cheapest as a spec edit at the next revision, which is outside this PBI's areas and the owner's call."
    },
    {
      "ref": "CR2-005-02 (LOW) - narrow the except OSError clause to the connection-loss errnos, or record the close-plus-stderr behaviour in spec section 6.3, and loosen the test to pin the stderr line rather than the absence of a response."
    },
    {
      "ref": "CR2-005-04 (LOW) - drop the unused _orig_poll from Live.setUp."
    },
    {
      "ref": "Builder-logged follow-up: send_error never calls log_error, so spec section 4.3's framework-diagnostic claim is unmet. Same subject as CR2-005-01; the builder disclosed it in change-report-r2 before the reviewer raised it independently."
    },
    {
      "ref": "Owner question Q-13 (the page fetches Google fonts, so it is not offline-capable) remains parked from the spec gate. Non-blocking; log an offline-fonts follow-up against site/** (PBI-006 or a new PBI)."
    },
    {
      "ref": "Owner question Q-17 (CLAUDE.md and README.md are not in PBI-005's allowed_areas, though the parent spec grants every PBI that touch) remains parked from the spec gate. Owner to add CLAUDE.md to allowed_areas or leave the note to PBI-007."
    }
  ]
}
