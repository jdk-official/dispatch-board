---
pbi_id: PBI-008
pbi_title: "Stale-board warning: 'data as of' header (feature 1; FR-103 refresher half, FR-104, FR-105)"
handover_type: done
date: 2026-09-12
---

# Handover — Stale-board warning

---

## Cell run (autonomy audit)

Human-in-loop, at the assisted tier, in session 7e0c4f3c. No spec gate (`requires_spec: false`); the criteria were firmed from the owner-confirmed parent-spec row 12 before the build.
- **Round 1:** `code-writer` built the refresher half but returned NOT-DONE at a blocked-area stop: `local/tests/test_conformance.py` had to learn the new record, and `local/**` was blocked. Recorded as an eroding build and paused.
- **Owner approval:** the scope was widened to that one file, verbatim "ok and approved. Continue building", with the rest of `local/` named file by file in `blocked_areas`. The scope manifest was re-synced from the main checkout's PBI file, since `scope_guard sync` had read the worktree's older copy.
- **Round 2:** `code-writer` returned DONE; `code-reviewer` returned GO-WITH-CONDITIONS (1 Medium: the page checks never proved the rendered time came from the record; 1 Low: a corrupt `at` rendered as a 1970 date instead of nothing; 2 Low naming and wording).
- **Round 3:** all four fixed test-first, the strengthened check confirmed to fail against the defect it pins; `code-reviewer` returned GO, re-proving both fixes by sabotaging scratch copies.
- **Canonical run:** 307 / 96 / 90 PASS, bound to `bd280d5`; `cell-report.json` CELL-DONE.

---

## Variant A — Done handover

### Effective trust tier

**Effective trust tier:** assisted. Gates: plan human, spec agent (not triggered), external_review human (not triggered), code_review agent, merge human.
**Effective-tier source:** `autonomy_resolver.py` — standard class, ratified lift `supervised`, capped by `autonomy_ceiling = "assisted"`.

### PR reference

<https://github.com/jdk-official/dispatch-board/pull/16>

### Merge commit short-SHA

`f2e58e6`, squash-merged by the owner at 2026-09-12T07:13:46Z.

- **resolved_method:** squash (declared).
- **observed_method:** squash.
- **Level:** record.

### Evidence path

- **Review note:** `docs/backlog/reviews/PBI-008/findings.json` (round 2 GO, 2 Low recorded), round 1 in `findings-r1.json`.
- **Change reports:** `change-report.json` (the blocked-area stop), `change-report-r2.json`, `change-report-r3.json`.
- **Run-report:** `run-report.json`, bound to `bd280d5`: 307 / 96 / 90, PASS. **Cell-report:** `cell-report.json`, CELL-DONE.
- **Acceptance evidence:** the Evidence section of `docs/backlog/pbi/PBI-008-stale-board.md`. Finalize re-ran the suites on merged `main` (`f2e58e6`): 307 / 96 / 194.

### Outcome summary

The board now says when its data was last refreshed, and when that has gone stale.
- Every refresh plan carries one `set` of `meta/lastRefresh`, holding the time of the write and `writer: "refresher"` — FR-103's refresher half, pairing with the collector's half in PBI-019.
- It never bumps a project's status, is never deleted by a plan, and never counts toward the mass-delete guard; a refused plan still writes and plans nothing.
- The page header shows "data as of <time>", turning amber with "stale" past 20 minutes, re-checked by the existing minute timer, and silent when the record is absent or unreadable.

**Live effect:** a tick now always has at least one write, so `refresh.py` never prints "nothing to push" again.

**Follow-ups, outside this PBI's areas:** CLAUDE.md's refresh procedure, its `/loop` prompt and its store-path table, and PRD FR-46, all still describe the removed output; the store-path table also needs a `meta/lastRefresh` row. Recorded as a docs chore. Two Lows from the round-2 review are recorded in the PBI's Notes.

**Unblocks:** the page group — PBI-009 to PBI-012 and PBI-020 — once this ledger row is deregistered.

---

## Handoff block (machine-validated — tools/handover_schema.py)

```json
{
  "schema_version": "1",
  "pbi_id": "PBI-008",
  "pbi_title": "Stale-board warning: 'data as of' header (feature 1; FR-103 refresher half, FR-104, FR-105)",
  "handover_type": "done",
  "as_of": "2026-09-12",
  "done": {
    "pr_url": "https://github.com/jdk-official/dispatch-board/pull/16",
    "merge_sha": "f2e58e6",
    "review_note_path": "docs/backlog/reviews/PBI-008/findings.json"
  }
}
```
