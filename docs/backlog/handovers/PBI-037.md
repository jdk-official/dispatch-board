# Handover — PBI-037: Retire the v1 artifact and its refresher: final store export, final status message, loop stopped, procedures marked retired (S-40)

PR [#42](https://github.com/jdk-official/dispatch-board/pull/42) squash-merged as `85654f8` on 2026-09-19. Done. Landing: `resolved_method` squash (**declared**), `observed_method` squash, level `record`.

The published claude.ai artifact is frozen: `meta/status` says "Retired 2026-09-19: the board now runs on this PC at http://127.0.0.1:8765" and `live` is false everywhere. Nothing republishes it, runs the refresher or writes its store; the only exception is the owner-only revert (restart the refresh loop), which stays possible until PBI-038 removes the push path. PBI-038 may start once PBI-007's AC-65 log-on demonstration is recorded and 7 days have passed (earliest 2026-09-26).

## Handoff block (machine-validated — tools/handover_schema.py)

```json
{
  "schema_version": "1",
  "pbi_id": "PBI-037",
  "pbi_title": "Retire the v1 artifact and its refresher: final store export, final status message, loop stopped, procedures marked retired (S-40)",
  "handover_type": "done",
  "as_of": "2026-09-19",
  "done": {
    "pr_url": "https://github.com/jdk-official/dispatch-board/pull/42",
    "merge_sha": "85654f8",
    "review_note_path": "docs/backlog/reviews/PBI-037/findings.json"
  }
}
```
