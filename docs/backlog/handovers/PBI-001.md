---
pbi_id: PBI-001
pbi_title: "Exporter hardening: FR-80–FR-82, FR-84, carried-tab marker, config type checks, refuse an answers collection (FR-133)"
handover_type: done
date: 2026-09-11
---

# Handover — Exporter hardening

---

## Cell run (autonomy audit)

The session was human-in-loop, at the assisted tier.
- **Round 1:**
  - `engineering-agents:code-writer` returned DONE-WITH-CONDITIONS, with 37 tests added;
  - `review-agents:code-reviewer` returned **NO-GO**, with 1 High: a verifier that declared a fallback could be published as `exercised`. It also found 1 Medium and 2 Low.
- **Round 2:**
  - the branch was rebased onto `d9de491` (PR #8) through a temporary commit, with no conflicts;
  - code-writer fixed all four findings test-first, adding 9 tests;
  - code-reviewer returned **GO-WITH-CONDITIONS**, with 2 Medium (the verifier tier order, and Windows-unsafe manual ids) and 1 Low (Later text after a blank line).
- **Round 3 (conditions only):** code-writer returned DONE, with 6 tests added. The review rounds were used up (`code_review_max_rounds` = 2), so the orchestrator checked the conditions with independent probes against `main`, as the reviewer allowed; see `docs/backlog/reviews/PBI-001/conditions-check.md`.

---

## Variant A — Done handover

### Effective trust tier

**Effective trust tier:** assisted. Gates: plan human, spec agent (not triggered), external_review human (not triggered), code_review agent, merge human.
**Effective-tier source:** baseline. The class's ratified lift is `supervised`, capped by `autonomy_ceiling = "assisted"`.

### PR reference

<https://github.com/jdk-official/dispatch-board/pull/10>

### Merge commit short-SHA

`d4023c6`, squash-merged by the owner (jdk-official) at 2026-09-11T16:30:15Z.

- **resolved_method:** squash (declared).
- **observed_method:** squash.
- **Level:** record.

### Evidence path

- **Review note:** `docs/backlog/reviews/PBI-001/findings.json` (round 2 GO-WITH-CONDITIONS), with round 1 in `findings-r1.json` and the conditions check in `conditions-check.md`.
- **Run-report:** `docs/backlog/reviews/PBI-001/run-report.json`, bound to `9aeff32`: 267 / 57 / 90, PASS.
- **Acceptance evidence:** the Evidence section of `docs/backlog/pbi/PBI-001-exporter-hardening.md`, covering suites 267 / 68 / 90 on `d4023c6`, the named tests for each criterion, and a real-data refresh with no warnings.

### Outcome summary

The exporters are hardened against bad input and honest about what they publish.
- **AC-57:** a stuck `running` row becomes `killed` / `no result`.
- **AC-58:** bad token counts no longer drop a session.
- **AC-59:** config type errors exit 2 with the key named.
- **AC-61:** a verifier's outcome is recorded honestly; a fallback always outranks `exercised` unless the outcome is labelled.
- **AC-86:** `refresh.py` refuses an `answers` document, and the C-16 rule is in CLAUDE.md.
- **Also closed:**
  - credentials stripped from published remotes, and gh `--repo` taken from origin;
  - `runs.manual` validation, including ids that are safe as Windows file names;
  - `meta/lastRefresh` reserved;
  - Later lists that take any Markdown marker and keep nested text;
  - `RecursionError` skipped;
  - the FR-137 test, and the CLAUDE.md corrections.

**Not delivered, although the title names it:** the carried-tab marker (PRD FR-190 / AC-125, `carriedSince` on a kept tab). It is not among this PBI's listed criteria, which predate PRD revision 3, and it was not built. It is carried to a small `exporters` follow-up PBI, to land before PBI-002's stale-tab callout and PBI-004's extraction. The other follow-ups are logged in PBI-001's Notes.

**Unblocks:** PBI-002 and PBI-004.

---

## Handoff block (machine-validated — tools/handover_schema.py)

```json
{
  "schema_version": "1",
  "pbi_id": "PBI-001",
  "pbi_title": "Exporter hardening: FR-80–FR-82, FR-84, carried-tab marker, config type checks, refuse an answers collection (FR-133)",
  "handover_type": "done",
  "as_of": "2026-09-11",
  "done": {
    "pr_url": "https://github.com/jdk-official/dispatch-board/pull/10",
    "merge_sha": "d4023c6",
    "review_note_path": "docs/backlog/reviews/PBI-001/findings.json"
  }
}
```
