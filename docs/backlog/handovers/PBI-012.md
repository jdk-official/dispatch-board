# Handover — PBI-012: Test and coverage trend, cost per work item and per agent type (features 5 and 7, FR-114–FR-116, FR-119, FR-120)

PR [#34](https://github.com/jdk-official/dispatch-board/pull/34) squash-merged as `312e2f5` on 2026-09-19. Done. Landing: `resolved_method` squash (**declared**), `observed_method` squash, level `record`.

Runs now carry a stated `tests` count and `coverage` (from builder results), the Dispatch tab charts both over the run log, and the usage tab shows cost per work item and per agent type. Accounted `code-review-r1` was GO with one Low: CR-012-1, stated-figure parsing crossing a line break, dispositioned follow-up and fixed as its own chore. Canonical run bound to `51acfa5`: 823 / 0.

Follow-up: `CLAUDE.md` does not yet document the new `tests`, `coverage` and per-PBI usage fields (docs chore).

## Handoff block (machine-validated — tools/handover_schema.py)

```json
{
  "schema_version": "1",
  "pbi_id": "PBI-012",
  "pbi_title": "Test and coverage trend, cost per work item and per agent type (features 5 and 7, FR-114–FR-116, FR-119, FR-120)",
  "handover_type": "done",
  "as_of": "2026-09-19",
  "done": {
    "pr_url": "https://github.com/jdk-official/dispatch-board/pull/34",
    "merge_sha": "312e2f5",
    "review_note_path": "docs/backlog/reviews/PBI-012/findings.json"
  }
}
```
