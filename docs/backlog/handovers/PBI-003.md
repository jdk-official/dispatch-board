---
pbi_id: PBI-003
pbi_title: "Record shapes and SQLite schema: session, run, project, tab, status, last-refresh and catalogue records (FR-100 except the answer record; FR-101 fields; FR-102)"
handover_type: done
date: 2026-09-11
---

# Handover — Record shapes and SQLite schema

**AC-69 disposition:** N/A for PBI-003, accepted by the owner on 2026-09-11 ("accept AC-69 as N/A for PBI-00"). It is verified in PBI-005, and PBI-003 contributed through AC-D2.

---

## Cell run (autonomy audit)

The session was human-in-loop, at the assisted tier.
- **Spec gate** (`pbi-review`, a clean-context Plan subagent): three rounds.
  - Round 1: CHANGES-REQUIRED.
  - Round 2: CHANGES-REQUIRED.
  - Round 3: APPROVE-WITH-NOTES, with every note applied in revision 4.
- **Build cell:**
  - `engineering-agents:code-writer` built it: DONE-WITH-CONDITIONS, with 81 tests;
  - `review-agents:code-reviewer` round 1: NO-GO, with one HIGH (a byte-exact test failed on a CRLF checkout) and 4 Low;
  - code-writer fixed all five: DONE, with 90 tests;
  - code-reviewer round 2: GO, with no findings.
- **Line endings:** proven on a real `core.autocrlf=true` clone.

---

## Variant A — Done handover

### Effective trust tier

**Effective trust tier:** assisted. Gates: plan human, spec agent, external_review human (not triggered), code_review agent, merge human.
**Effective-tier source:** baseline, capped by `autonomy_ceiling = "assisted"`.

### PR reference

<https://github.com/jdk-official/dispatch-board/pull/7>

### Merge commit short-SHA

`7fd96cb`, squash-merged by the owner (jdk-official) at 2026-09-11T15:03:45Z.

- **resolved_method:** squash (declared).
- **observed_method:** squash.
- **Level:** record.

### Evidence path

- **Review note:** `docs/backlog/reviews/PBI-003/findings.json` (round 2, GO), with round 1 in `findings-r1.json`.
- **Spec:** `docs/backlog/specs/pbi-003-records-schema.md` revision 4. Its spec reviews are `spec-review-r1..r3.md`.

### Outcome summary

The local-first app now has its one record definition and its database layout.
- **`local/records.py`:** `SHAPES`, `validate`, the id forms, `store_path`, and `to_row`/`from_row`.
- **`local/records.shapes.json`:** the JSON copy for the page's adapters.
- **`local/schema.py`:** 7 tables, and an atomic, idempotent `create_schema`.
- **Tests:** 90 in `local/tests/`, including conformance of every v1 exporter document.
- **Unblocks:** PBI-019, PBI-005, PBI-006, PBI-008, PBI-014 and PBI-020, together with PBI-004 where they need it.
- **Follow-ups** are logged in PBI-001, PBI-005, PBI-006 and PBI-019.
- **`[test_commands]`:** done in PR #8 (`d9de491`). `local/tests` is in `[test_commands]`, with the Python 3.11+ minimum.

---

## Handoff block (machine-validated — tools/handover_schema.py)

```json
{
  "schema_version": "1",
  "pbi_id": "PBI-003",
  "pbi_title": "Record shapes and SQLite schema: session, run, project, tab, status, last-refresh and catalogue records (FR-100 except the answer record; FR-101 fields; FR-102)",
  "handover_type": "done",
  "as_of": "2026-09-11",
  "done": {
    "pr_url": "https://github.com/jdk-official/dispatch-board/pull/7",
    "merge_sha": "7fd96cb",
    "review_note_path": "docs/backlog/reviews/PBI-003/findings.json"
  }
}
```
