---
pbi_id: PBI-023
pbi_title: "Pull requests on the board: the GitHub tab lists each project's pull requests as links, and Needs attention flags the ones awaiting the owner's merge"
handover_type: done
date: 2026-09-11
---

# Handover — Pull requests on the board

---

## Cell run (autonomy audit)

The session was human-in-loop, at the assisted tier.
- **Round 1:**
  - `engineering-agents:code-writer` returned DONE-WITH-CONDITIONS: the real `gh` hadn't been run and the page hadn't been viewed;
  - the orchestrator closed both conditions: a real `gh` run gave 5 PRs, and a Browser-pane check passed;
  - `review-agents:code-reviewer` returned GO-WITH-CONDITIONS, with 2 Medium findings and 1 Low.
- **Round 2:** code-writer applied all three conditions, test-first, and proved them with a mutation check.

---

## Variant A — Done handover

### Effective trust tier

**Effective trust tier:** assisted. Gates: plan human, spec agent (not triggered), external_review human (not triggered), code_review agent, merge human.
**Effective-tier source:** baseline. The ratified lift is `supervised`, capped by `autonomy_ceiling = "assisted"`.

### PR reference

<https://github.com/jdk-official/dispatch-board/pull/6>

### Merge commit short-SHA

`547b635`, squash-merged by the owner (jdk-official) at 2026-09-11T15:03:35Z.

- **resolved_method:** squash (declared).
- **observed_method:** squash.
- **Level:** record.

### Evidence path

- **Review note:** `docs/backlog/reviews/PBI-023/findings.json` (GO-WITH-CONDITIONS; CR-PBI023-01 to 03 applied in `e17006b`).
- **Smoke-test evidence:**
  - a real `gh` run returned 5 PRs;
  - the Browser-pane check passed: the GitHub panel links open in a new tab with `noopener noreferrer`, the Repository box read "1 open", and Needs attention showed "PR #5 awaiting your merge" in `--human`;
  - the post-merge refresh published `pulls` with 7 PRs.

  All of this is recorded in `docs/backlog/pbi/PBI-023-pr-links.md` Evidence.

### Outcome summary

The GitHub tab now lists each GitHub project's 20 most recently updated pull requests, as links.
- **Needs attention:** every open pull request shows as "PR #n awaiting your merge".
- **Failure handling:** a missing or failing `gh` never breaks the refresh, and a project without a GitHub remote is left alone.
- **Links:** only `https://github.com/` URLs become links.
- **Live:** the page was republished and the `pulls` data pushed on 2026-09-11.
- **Follow-ups** are logged in:
  - PBI-001: userinfo in `remotes`, `--repo`, drafts, and the 20-row window;
  - PBI-002: the other label lookups.

**One check still to make on the live artifact:** whether its sandbox opens the links in a new tab. If it doesn't, each link still shows its URL.

---

## Handoff block (machine-validated — tools/handover_schema.py)

```json
{
  "schema_version": "1",
  "pbi_id": "PBI-023",
  "pbi_title": "Pull requests on the board: the GitHub tab lists each project's pull requests as links, and Needs attention flags the ones awaiting the owner's merge",
  "handover_type": "done",
  "as_of": "2026-09-11",
  "done": {
    "pr_url": "https://github.com/jdk-official/dispatch-board/pull/6",
    "merge_sha": "547b635",
    "review_note_path": "docs/backlog/reviews/PBI-023/findings.json"
  }
}
```
