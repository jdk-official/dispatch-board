---
pbi_id: PBI-018
pbi_title: "Backlog shows future iterations: a 'Later' group of idea cards read from the spec's Future iterations list"
handover_type: done
date: 2026-09-11
---

# Handover — Later group on the Backlog tab

---

## Cell run (autonomy audit)

The session was human-in-loop, at the assisted tier.
- **Round 1:**
  - `engineering-agents:code-writer` returned DONE-WITH-CONDITIONS, the condition being that the page had not been viewed in a browser;
  - the separate test-writer step was skipped, because TDD had already produced 13 new tests and the reviewer judges their adequacy;
  - `review-agents:code-reviewer` returned GO, with one LOW.
- **Browser check:** the orchestrator viewed the page in the Browser pane and confirmed the neutral tone bar.

---

## Variant A — Done handover

### Effective trust tier

**Effective trust tier:** assisted. Gates: plan human, spec agent (not triggered), external_review human (not triggered), code_review agent, merge human.
**Effective-tier source:** baseline. The class's ratified lift is `supervised`, capped by `autonomy_ceiling = "assisted"`.

### PR reference

<https://github.com/jdk-official/dispatch-board/pull/4>

### Merge commit short-SHA

`25fa9e7`, squash-merged by the owner (jdk-official) at 2026-09-11T13:40:23Z.

- **resolved_method:** squash (declared).
- **observed_method:** squash (1 parent, 1 landed commit).
- **Level:** record.

### Evidence path

- **Review note:** `docs/backlog/reviews/PBI-018/findings.json`, verdict GO. CR-018-01 (LOW) is deferred to PBI-001: only `- ` bullets are recognised, and that matches the real spec.
- **Smoke-test evidence:** the page was checked in the Browser pane from the branch with the real exported data.
  - 9 idea cards render below Work items.
  - Their tone bar resolves to `--rule-2`, not `--human`.
  - The page logged no errors.

  This is recorded in the Evidence section of `docs/backlog/pbi/PBI-018-later-group.md`.

### Outcome summary

The Backlog tab now shows the spec's "Future iterations (not planned)" list as a "Later" group of neutral idea cards below the work items.
- **The 9 ideas include:** answering assumptions from the board, phone notifications, hosting on Azure, and archiving beyond 7 days.
- **Exporter:** `exporters/export_board.py` adds a `later` list to each project's backlog document. Platform-catalogue's spec has no such heading, so it gets `[]`.
- **Page:** it never counts `later` in the PBI totals, the Overview tiles, the cells, the dependency graph or Needs attention.
- **Tests:** 8 exporter tests and 5 page checks were added.
- **Live:** shipped on 2026-09-11. The data (with `later`) was pushed and the page republished to the same artifact URL.
- **Unblocks:** PBI-001, PBI-003 and PBI-013, and it frees the page group for PBI-023.

---

## Handoff block (machine-validated — tools/handover_schema.py)

```json
{
  "schema_version": "1",
  "pbi_id": "PBI-018",
  "pbi_title": "Backlog shows future iterations: a 'Later' group of idea cards read from the spec's Future iterations list",
  "handover_type": "done",
  "as_of": "2026-09-11",
  "done": {
    "pr_url": "https://github.com/jdk-official/dispatch-board/pull/4",
    "merge_sha": "25fa9e7",
    "review_note_path": "docs/backlog/reviews/PBI-018/findings.json"
  }
}
```
