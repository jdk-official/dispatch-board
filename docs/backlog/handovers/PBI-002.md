---
pbi_id: PBI-002
pbi_title: "Page fixes: FR-83 live-state tile, stale-tab callout, token relabel, design-rule deviations, blank-load investigation (FR-85)"
handover_type: done
date: 2026-09-11
---

# Handover — Page fixes

**Candidate — close-out in progress (finalize step 4 of 10).**

---

## Cell run (autonomy audit)

The session was human-in-loop, at the assisted tier, and the build ran in session 9562c312. Round 1 code review was GO-WITH-CONDITIONS, and both Lows were applied; round 2 was GO. The canonical run was bound to `d10aa68`: 267 / 89 / 90, PASS. The PR was pushed via `git_rail.py`. Session 7e0c4f3c ran the finalize.

---

## Variant A — Done handover

### Effective trust tier

**Effective trust tier:** assisted. Gates: plan human, spec agent (not triggered), external_review human (not triggered), code_review agent, merge human.
**Effective-tier source:** baseline. The class's ratified lift is `supervised`, capped by `autonomy_ceiling = "assisted"`.

### PR reference

<https://github.com/jdk-official/dispatch-board/pull/12>

### Merge commit short-SHA

`049f27e`, squash-merged by the owner at 2026-09-11T17:45:06Z.

- **resolved_method:** squash (declared).
- **observed_method:** squash.
- **Level:** record.

### Evidence path

- **Review note:** `docs/backlog/reviews/PBI-002/findings.json` (round 2 GO), with round 1 in `findings-r1.json`.
- **Run-report:** `docs/backlog/reviews/PBI-002/run-report.json`, bound to `d10aa68`.
- **Acceptance evidence:** the Evidence section of `docs/backlog/pbi/PBI-002-page-fixes.md`. The page suite passed all 89 checks on merged `main`.
- **AC-62:** the live 20-load check stopped at 5 loads, run in Edge. Four loads drew within 5 s; one showed an empty page area at 5 s, undetermined. The owner accepted it on 2026-09-11 ("accept but keep the test active"). The check stays active and is re-run when a visible browser is available.

### Outcome summary

The page fixes are live on the board:
- "Running now" says "session idle" when the header turns Idle (FR-83).
- A project tab kept from an earlier export warns with its `carriedSince` time (FR-191).
- The brand dot no longer uses `--human`.
- The header dot pulses only while an agent runs.
- Paths and branch names use the sans face.
- Every token figure is labelled "reported tokens".
- The page follow-ups logged by earlier reviews are fixed.
- The likely cause of the blank first load (FR-85), a render-blocking font stylesheet, is fixed.

**Still active:** the AC-62 20-load live check, as a standing re-run. A load still blank at 12 s reopens FR-85.

**Unblocks:** the page group. PBI-008 to PBI-012 and PBI-020 can now start, one page PBI at a time.

---

## Handoff block (machine-validated — tools/handover_schema.py)

```json
{
  "schema_version": "1",
  "pbi_id": "PBI-002",
  "pbi_title": "Page fixes: FR-83 live-state tile, stale-tab callout, token relabel, design-rule deviations, blank-load investigation (FR-85)",
  "handover_type": "done",
  "as_of": "2026-09-11",
  "done": {
    "pr_url": "https://github.com/jdk-official/dispatch-board/pull/12",
    "merge_sha": "049f27e",
    "review_note_path": "docs/backlog/reviews/PBI-002/findings.json"
  }
}
```
