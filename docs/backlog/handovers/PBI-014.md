# Handover — PBI-014: Run detail (feature 8, FR-121)

PR [#24](https://github.com/jdk-official/dispatch-board/pull/24) squash-merged as `4bbbad8` on 2026-09-13. Closed out 2026-09-19 once the owner approved AC-82 and NFR-22 on the rendered screenshots ("ok, approved"). Its Done status releases the `page` group's High-risk slot and unblocks PBI-027, and through it PBI-007.

## Landing

| Field | Value |
|---|---|
| Merge SHA | `4bbbad8` |
| `resolved_method` | `squash` (provenance: **declared**) |
| `observed_method` | `squash` |
| Level | `record` |

Merged by the orchestrator under the owner's standing authorisation of 2026-09-12, verbatim: "Standing authorisation: merge any PBI PR yourself, squash, once its code review is GO or its conditions are all applied. Don't ask me each time." The final review was GO-WITH-CONDITIONS and every merge condition was applied first.

**A defect in the orchestrator's own merge step, recorded honestly:** the pre-merge "is it mergeable?" guard read `{"mergeStateStatus":"UNKNOWN","mergeable":"UNKNOWN"}` yet passed, because PowerShell's `-notmatch` is case-insensitive and matched the lowercase *key* `"mergeable"` rather than its value. The merge was nonetheless safe — GitHub reports UNKNOWN immediately after a PR is created, and the branch base had been confirmed equal to `origin/main` just before the canonical run, so no conflict was possible — and it landed cleanly. The guard checked nothing, and has been replaced by JSON parsing for later merges.

## What landed

Picking a run on the Dispatch tab opens its detail: the verdict summary, the findings its review round listed, the files the run edited, and its duration. Run documents gained two optional fields — the round's own `findings`, and `files` (paths relative to the project's `repoPath`; an unplaceable path published as `…/<file name>` with its folders withheld; refused or failed edits not counted). `PARSER_VERSION` moved 6 → 7.

## Gates

| Gate | Outcome |
|---|---|
| Spec gate | n/a (`requires_spec: false`) |
| External-review gate | n/a |
| Code-review gate | Round 1 **NO-GO** (1 High, 2 Medium, 3 Low), fixed; round 2 (final) **GO-WITH-CONDITIONS** (3 Medium, 1 Low), conditions applied |
| No-self-merge gate | Passed — merged under the standing owner authorisation, conditions applied |
| BOARD-tidy gate | Passed 2026-09-19 — AC-82 and NFR-22 approved by the owner ("ok, approved") against the rendered screenshots in `docs/backlog/evidence/PBI-014/` |

## Evidence

Suites on the merged head `4bbbad8`: `tests` **360**, `page.test.mjs` **121**, `local/tests` **304** — all OK. Canonical run bound to `123a61c`, taken on a base confirmed equal to `origin/main`, with the commit following the run in the same step and porcelain empty afterwards.

## What is worth carrying forward

**A reversal of a merged PBI's decision, judged legitimate.** PBI-011 had pinned "run documents carry no findings". The round-1 reviewer traced PBI-011's record and found that test guarded a scope boundary, not an architecture — and that the findings ledger cannot answer "this run's findings" because it keys findings by round ordinal, not run id.

**A requirement declared undeliverable that was deliverable.** The first build said "files touched" could not be built. Round 1 disproved both reasons at source: the transcript walk already read tool inputs, and the board already published absolute session paths. The orchestrator had relayed the builder's claim to the owner as a recommendation, and withdrew it.

**What "files touched" honestly delivers,** measured on real transcripts by the final reviewer: code-reviewer runs make **0 edits** (0 of 37), so for the run type AC-82 names, the panel shows the files a run's *findings* name; and **65 of dispatch-board's 140** recorded edits were made in git worktrees outside `repoPath`, so they show by file name only. For main-checkout work it is precise (platform-catalogue: 110 repo-relative against 10 withheld).

**The fix round was interrupted by a usage limit** mid-edit, leaving the tree failing one test. The resuming run diagnosed it (the exporter never passed `repoPath` to `run_doc`) and found the interrupted run's parser-version test would have passed with no bump; it re-wrote the test to bite.

## Follow-ups

- **PBI-027** — `local/collector.py:436` omits the repository that `exporters/export_sessions.py:286` passes, so the local database would store `…/index.html` where the board stores `site/index.html`, breaking the rule that both derive identical documents. **PBI-007 now depends on PBI-027**, so the deployment cannot ship first.
- **Docs chore (CR-014-3)** — `CLAUDE.md`'s `runs/<id>` row and privacy section name `findings` and `files`. Merged as `47f8c13` (PR #25).
- **The owner's NFR-22 confirmation** — given 2026-09-19 ("ok, approved") on four rendered screenshots, both themes. Done.

## Handoff block (machine-validated — tools/handover_schema.py)

```json
{
  "schema_version": "1",
  "pbi_id": "PBI-014",
  "pbi_title": "Run detail (feature 8, FR-121)",
  "handover_type": "done",
  "as_of": "2026-09-13",
  "done": {
    "pr_url": "https://github.com/jdk-official/dispatch-board/pull/24",
    "merge_sha": "4bbbad8",
    "review_note_path": "docs/backlog/reviews/PBI-014/findings.json"
  }
}
```
