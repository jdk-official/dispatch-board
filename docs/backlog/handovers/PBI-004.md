---
pbi_id: PBI-004
pbi_title: "Shared derivation module: extract the exporters' parsing and derivation into an importable module, behaviour unchanged"
handover_type: done
date: 2026-09-11
---

# Handover — Shared derivation module

---

## Cell run (autonomy audit)

The session was human-in-loop, at the assisted tier. The worker was session 7e0c4f3c, and the owner's instruction to start was "This is for dispatch board. Lets keep building the backlog".
- **Criteria firmed before the build:** the PBI's only feature criterion pointed to a per-PBI spec that does not exist (`requires_spec: false`). The worker firmed D-1 to D-5 from the title, the parent spec's Key decisions and PRD FR-87 / C-21, adding no scope.
- **Round 1:**
  - `engineering-agents:code-writer` returned DONE: `exporters/derive.py`, with 21 new tests, and output byte-identical across 155 files.
  - `review-agents:code-reviewer` returned **GO-WITH-CONDITIONS**, with 1 Medium and 2 Low.
  - The Medium: returned documents shared live `skillUses` and `rejects` with the ongoing derivation, so a collector feeding more records would see earlier documents change.
- **Round 2:**
  - code-writer fixed all three findings test-first. It also copied each response's `tools` list, which it found aliased too.
  - `use_skill` was removed, since nothing called it.
  - Output stayed byte-identical across 157 files.
  - code-reviewer returned **GO**, with 1 Low: a stale comment. The orchestrator reworded it before the canonical run.
- **Canonical run:** 300 / 89 / 90 PASS, bound to `333361b` (`run-report.json`); `cell-report.json` is CELL-DONE.
- **Push:** via `git_rail.py`. Hosted CI: none (`ci.provider` none).

---

## Variant A — Done handover

### Effective trust tier

**Effective trust tier:** assisted. Gates: plan human, spec agent (not triggered), external_review human (not triggered), code_review agent, merge human.
**Effective-tier source:** `autonomy_resolver.py`, run with change class standard, `autonomy_ceiling = "assisted"` and the class's ratified earned lift `supervised`. The ceiling caps the lift at assisted.

### PR reference

<https://github.com/jdk-official/dispatch-board/pull/14>

### Merge commit short-SHA

`d10d9e6`, squash-merged by the owner at 2026-09-11T19:35:50Z.

- **resolved_method:** squash (declared).
- **observed_method:** squash.
- **Level:** record.

### Evidence path

- **Review note:** `docs/backlog/reviews/PBI-004/findings.json` (round 2 GO), with round 1 in `findings-r1.json`.
- **Change reports:** `change-report-r1.json` and `change-report-r2.json`.
- **Run-report:** `docs/backlog/reviews/PBI-004/run-report.json`, bound to `333361b`: 300 / 89 / 90, PASS.
- **Cell-report:** `docs/backlog/reviews/PBI-004/cell-report.json` (CELL-DONE).
- **Acceptance evidence:** the Evidence section of `docs/backlog/pbi/PBI-004-shared-derivation.md`. Finalize re-ran the three suites on merged `main` (`d10d9e6`): 300 / 89 / 90. A real-data refresh with the new exporters planned no change to any session, run, project, catalogue or tab, other than the git tab (a new commit) and this session's own document.

### Outcome summary

The exporters' parsing and derivation now live in one shared module, `exporters/derive.py`.
- It is standard library only, and importing it does no I/O.
- Its functions take parsed records and text rather than file paths, so the collector (PBI-019) can import it and feed it records as it reads them incrementally (PRD FR-86, FR-87).
- Returned documents are snapshots.
- The three exporters now read their inputs and call it, and their output is unchanged.
- One edge differs: a spec parse error together with an unreadable ADR file now prints a different error and exits 2 instead of 1. `refresh.py` pushes nothing in both cases.

**Unblocks:** PBI-019 (collector), PBI-005 (local server), PBI-008 to PBI-012 and PBI-020 (page features).

---

## Handoff block (machine-validated — tools/handover_schema.py)

```json
{
  "schema_version": "1",
  "pbi_id": "PBI-004",
  "pbi_title": "Shared derivation module: extract the exporters' parsing and derivation into an importable module, behaviour unchanged",
  "handover_type": "done",
  "as_of": "2026-09-11",
  "done": {
    "pr_url": "https://github.com/jdk-official/dispatch-board/pull/14",
    "merge_sha": "d10d9e6",
    "review_note_path": "docs/backlog/reviews/PBI-004/findings.json"
  }
}
```
