# Handover — PBI-027: Local collector publishes run files relative to the repository

PR [#27](https://github.com/jdk-official/dispatch-board/pull/27) squash-merged as `941ac4b` on 2026-09-19. Done.

## Landing

| Field | Value |
|---|---|
| Merge SHA | `941ac4b` |
| `resolved_method` | `squash` (provenance: **declared**) |
| `observed_method` | `squash` |
| Level | `record` |

Merged by the orchestrator under the owner's standing authorisation of 2026-09-12 ("merge any PBI PR yourself, squash, once its code review is GO or its conditions are all applied").

## What landed

The local collector passes each run's project `repoPath` to `derive.run_doc`, built from the exporter's own settings, so a run's `files` in the local database equals the board's. The run record shape names the optional `files` and `findings` fields.

## Gates

| Gate | Outcome |
|---|---|
| Code-review gate | Accounted dispatch `code-review-r1` (Opus, fresh context) on `5ad02c8`: GO, 0 findings. Allowance 2, 1 used |
| No-self-merge gate | Passed under the standing owner authorisation |
| BOARD-tidy gate | Passed |

## Evidence

Canonical run via `round_close run`, bound to `5ad02c8`: backend 360, local 306, page suite exit 0 (121 checks). Evidence validation PASS at pre-review, gate-complete and pre-push. All under `docs/backlog/reviews/PBI-027/`.

## What is worth carrying forward

**The first PBI under bounded review accounting.** The agent-catalog plugin clone fast-forwarded on 2026-09-19 to a rail that fails closed without `review_gates[]` evidence and without a slugged PBI filename. The owner chose (1) a live re-review, with the earlier pre-accounting review logged as an event carrying its findings digest, and (2) one chore renaming all PBI files (PR #26). The procedure: commit the candidate, `round_close run`, write the `started` dispatch, `validate-evidence --phase pre-review`, dispatch the reviewer on the committed head, record the completed dispatch, `gate-complete`, `round_close bind`, `pre-push`, push. It now applies to every PBI.

## Follow-ups

- Docs chore: drop `CLAUDE.md`'s "Known gap until PBI-027" sentence.
- Non-blocking: check `files` items are strings; neutral profile path in a test fixture.
- PBI-007 is now dependency-clear, and its earlier review predates the accounting too.

## Handoff block (machine-validated — tools/handover_schema.py)

```json
{
  "schema_version": "1",
  "pbi_id": "PBI-027",
  "pbi_title": "Local collector publishes a run's files relative to the repository, so the local app matches the board",
  "handover_type": "done",
  "as_of": "2026-09-19",
  "done": {
    "pr_url": "https://github.com/jdk-official/dispatch-board/pull/27",
    "merge_sha": "941ac4b",
    "review_note_path": "docs/backlog/reviews/PBI-027/findings.json"
  }
}
```
