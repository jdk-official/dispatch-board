---
pbi_id: PBI-013
pbi_title: "Usage limit forecast (feature 6, FR-117, FR-118)"
handover_type: done
date: 2026-09-11
---

# Handover — Usage limit forecast

---

## Cell run (autonomy audit)

The session was human-in-loop, at the assisted tier.
- **Round 1:**
  - `engineering-agents:code-writer` returned DONE-WITH-CONDITIONS: the method was the writer's choice, because the spec names none;
  - `review-agents:code-reviewer` returned GO-WITH-CONDITIONS, with 1 Medium (idle time after a reset was counted, making the estimate about 3.5 h late on real data) and 4 Low.
- **Round 2:** code-writer applied all five, test-first; the page suite went from 63 to 68 checks. code-reviewer returned GO, with one new Low (CR-PBI013-06) deferred with rationale to PBI-002.
- **Rebase:** the branch was rebased onto `d9de491` (PR #8) before the canonical run, using a temporary commit; no conflicts.

---

## Variant A — Done handover

### Effective trust tier

**Effective trust tier:** assisted. Gates: plan human, spec agent (not triggered), external_review human (not triggered), code_review agent, merge human.
**Effective-tier source:** baseline. The class's ratified lift is `supervised`, capped by `autonomy_ceiling = "assisted"`.

### PR reference

<https://github.com/jdk-official/dispatch-board/pull/9>

### Merge commit short-SHA

`602eab9`, squash-merged by the owner (jdk-official) at 2026-09-11T16:13:06Z.

- **resolved_method:** squash (declared).
- **observed_method:** squash.
- **Level:** record.

### Evidence path

- **Review note:** `docs/backlog/reviews/PBI-013/findings.json` (round 2 GO), with round 1 in `findings-r1.json`.
- **Run-report:** `docs/backlog/reviews/PBI-013/run-report.json`, bound to `c54a119`: 215 / 68 / 90, PASS.
- **Acceptance evidence:** the Evidence section of `docs/backlog/pbi/PBI-013.md`, covering the fresh suites on `602eab9` and AC-80 checked against the live store's real data.

### Outcome summary

The board now estimates when the next usage limit will be hit, as a tile on the Claude usage tab and on the Overview, for the project or session in view.
- **Label:** it is always labelled an estimate, with a note on how it is worked out and that it is not a plan meter.
- **Method:** each recorded hit is timed from when work resumed (the later of the reset before it and the first hour with usage), and the mean gap is added to the latest reset.
- **With no hit recorded:** it reads "no forecast: no limit hit recorded".
- **Other states:** usage not exported yet, too little timing data, and a latest hit with no reset time.
- **Live:** the page was republished on 2026-09-11.

**Open items for the owner:**
- **Forecast method:** it is not written in the spec or PRD (ledger row 11 / PRD A-34 defer to a default that does not exist). Record the implemented method, or ask for a usage-rate model instead. It is logged in PBI-013's Notes.
- **Follow-ups for PBI-002:** CR-PBI013-06 (the passed-state check runs outside the per-tab guard) and the non-list `usage.hourly` crash in `usageChart`.
- **Docs:** `CLAUDE.md` does not describe the forecast yet; this goes in the next docs chore.

---

## Handoff block (machine-validated — tools/handover_schema.py)

```json
{
  "schema_version": "1",
  "pbi_id": "PBI-013",
  "pbi_title": "Usage limit forecast (feature 6, FR-117, FR-118)",
  "handover_type": "done",
  "as_of": "2026-09-11",
  "done": {
    "pr_url": "https://github.com/jdk-official/dispatch-board/pull/9",
    "merge_sha": "602eab9",
    "review_note_path": "docs/backlog/reviews/PBI-013/findings.json"
  }
}
```
