# Handover — PBI-029: No console windows from the scheduled collector

PR [#35](https://github.com/jdk-official/dispatch-board/pull/35) squash-merged as `8bd2621` on 2026-09-19. Done. Landing: `resolved_method` squash (**declared**), `observed_method` squash, level `record`.

Every git child the collector and server start passes `no_window_flags()` (`CREATE_NO_WINDOW` on Windows), so the log-on tasks no longer flash console windows. Proved with a real `pythonw.exe` parent and a console-subsystem child's `GetConsoleWindow()`. Accounted review r1–r3 GO, 0 findings; r3 ran under the owner's +1 dispatch extension. Canonical run bound to `a9985a7`: 826 / 0.

The owner restarts the scheduled tasks to pick it up: `python local/deploy/tasks.py start`.

## Handoff block (machine-validated — tools/handover_schema.py)

```json
{
  "schema_version": "1",
  "pbi_id": "PBI-029",
  "pbi_title": "No console windows from the scheduled collector",
  "handover_type": "done",
  "as_of": "2026-09-19",
  "done": {
    "pr_url": "https://github.com/jdk-official/dispatch-board/pull/35",
    "merge_sha": "8bd2621",
    "review_note_path": "docs/backlog/reviews/PBI-029/findings.json"
  }
}
```
