---
pbi_id: PBI-021
pbi_title: "Store leftovers clean-up: export the six unused documents to snapshot/, then delete them with the owner's explicit approval"
handover_type: done
date: 2026-09-11
---

# Handover — Store leftovers clean-up

---

## Cell run (autonomy audit)

The session was human-in-loop, at the assisted tier.
- **External-review gate:** the owner approved, verbatim: "approve PBI-021". That covered starting the work only.
- **Build cell:**
  - `engineering-agents:code-writer` refreshed `snapshot/tabs/` from the store's six retired documents;
  - `review-agents:code-reviewer` returned GO, with no findings.
- **The rail refused the first push,** because the worker's `findings.json` was malformed JSON. It was rewritten, validated and pushed.
- **The delete:**
  - it ran only after the owner's separate verbatim approval (AC-S2);
  - the orchestrator checked it against the committed snapshot before the batch, and checked the store and the page after it.

---

## Variant A — Done handover

### Effective trust tier

**Effective trust tier:** assisted. Gates: plan human, spec agent (not triggered), external_review human, code_review agent, merge human.
**Effective-tier source:** baseline, capped by `autonomy_ceiling = "assisted"`.

### PR reference

<https://github.com/jdk-official/dispatch-board/pull/2>

### Merge commit short-SHA

`fd7b926`, squash-merged by the owner (jdk-official) at 2026-09-11T13:40:50Z.

- **resolved_method:** squash (declared).
- **observed_method:** squash (1 parent, 1 landed commit).
- **Level:** record.

### Evidence path

- **Review note:** `docs/backlog/reviews/PBI-021/findings.json` (GO).
- **Acceptance evidence:** the Evidence section of `docs/backlog/pbi/PBI-021-store-cleanup.md`, covering AC-S1 to AC-S3 and the owner's verbatim delete approval.

### Outcome summary

The store no longer holds the six leftover documents from before project-first navigation: `tabs/usage`, `tabs/spec`, `tabs/assumptions`, `tabs/decisions`, `tabs/backlog` and `tabs/git`.
- **Owner's approval:** "approve deleting tabs/usage, tabs/spec, tabs/assumptions, tabs/decisions, tabs/backlog and tabs/git" (2026-09-11).
- **Copies:** they remain in `snapshot/tabs/` (committed in `fd7b926`), and each matched the store exactly before the delete. Restore any of them with `write_db` (`file_path` per document).
- **After the delete:** the store holds 140 documents, and the published page renders all 9 tabs for both projects with no errors.
- **Follow-up:** CLAUDE.md's "Retired" paragraph and its "Retired tabs" and "Leftover documents" open items still describe these documents as present. Update them in the next docs chore.

---

## Handoff block (machine-validated — tools/handover_schema.py)

```json
{
  "schema_version": "1",
  "pbi_id": "PBI-021",
  "pbi_title": "Store leftovers clean-up: export the six unused documents to snapshot/, then delete them with the owner's explicit approval",
  "handover_type": "done",
  "as_of": "2026-09-11",
  "done": {
    "pr_url": "https://github.com/jdk-official/dispatch-board/pull/2",
    "merge_sha": "fd7b926",
    "review_note_path": "docs/backlog/reviews/PBI-021/findings.json"
  }
}
```
